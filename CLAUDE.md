# Career Copilot Premium

AI interview assistant desktop app. PySide6 (floating overlay) + Flask (local dashboard). $299 product.

## Version

- Current version: **v2.1.0** — Ed25519 licensing security upgrade (see the `v2.1.0` tag entry below and the "Activation crypto hardened" item under Fix status). `CURRENT_APP_VERSION` in `web_app/routes.py`, `setup.py`, `career-copilot-version.txt`, and `installers/windows_setup.iss` bumped together; tag `v2.1.0` pushed with `main`, CI (`build-desktop-mac-linux.yml`) auto-triggered on the tag push to build + publish the GitHub release. Minor-version bump (not patch) because every activation code issued before this release stops verifying — both existing clients must re-activate.
- Previous: v2.0.8 — tag pushed, merged to `main`. `CURRENT_APP_VERSION` in `web_app/routes.py`, `setup.py`, `career-copilot-version.txt`, and `installers/windows_setup.iss` all bumped together in commit `7b3db3e0`. Unlike `v2.0.5`/`v2.0.6` (left in place, broken, superseded), the `v2.0.8` *tag* was deliberately **moved**: it was first pushed pointing at a CSS-only commit (`474572d1`) that hadn't bumped the version strings, then deleted and recreated on `7b3db3e0` once the version bump landed, so the tag and `CURRENT_APP_VERSION` actually match. The stale CI run triggered by the original tag push was cancelled before it could publish a release, so only one `v2.0.8` GitHub Release exists (built from `7b3db3e0`). If `v2.0.8` is ever checked out from reflog/local history before this note, be aware the tag ref changed after initial push — `git log --oneline` on `main` is authoritative, not muscle memory from earlier in this session.
- **Gap CLOSED as of v2.0.7** (CI-confirmed, run `29292805781`): `v2.0.1`–`v2.0.4`'s GitHub Releases had no Windows `.exe` attached (only `v2.0.0` did, built manually) — `build-desktop-mac-linux.yml` only had macOS + Linux jobs. Getting a working `build-windows` job took three attempts: `v2.0.5` added the job but it failed (missing gitignored `data/` scaffold files); `v2.0.6` fixed that but hit a second, different failure (missing gitignored `vc_redist.x64.exe`); `v2.0.7` fixed that too and is confirmed working — https://github.com/farazgoal-boop/career_copilot_premium/releases/tag/v2.0.7 has all four assets: `CareerCopilotPremium-mac.dmg`, `CareerCopilotPremium-linux.deb`, `CareerCopilotPremium-linux.AppImage`, `CareerCopilotPremium_Setup_v2.0.7.exe`. This is the first fully-automated Windows+Mac+Linux release since `v2.0.0` (which was Windows-manual). See the three tag entries below for the full blow-by-blow if `build-windows` ever breaks again.
- Git tags:
  - `v1.0.5` — macOS SSL cert fix
  - `v1.0.6` — activation/profile/sessions/mobile QR
  - `v1.0.8` — overlay error dialog, tray icon
  - `stable-backup-2026-07-06` — recovery point, do not delete
  - `phase1-complete-2026-07-07` — browser mic capture + live transcript + transcript export, 62/62 e2e tests passing, do not delete
  - `phase2-complete-2026-07-07` — Visual Context Library, session types, continuous recording, meeting summary, post-session report page. Merged to `main` (`--no-ff`), 62/62 e2e tests passing, do not delete
  - `v2.0.0` — Phase 3 complete (Steps 3.1–3.10: voice output, multi-language, themes, auto-update, company research, resume-matched answers, prepare mode, premium onboarding, page transitions, version bump, Windows packaging script fixes). Pushed, merged to `main` (`--no-ff`, merge commit `4f4db0c4`), GitHub release published with Windows/macOS/Linux assets. `feature/multilang-premium-polish` branch deleted (local-only, never existed on origin)
  - `v2.0.1` — Mac overlay reliability fixes on the existing PySide6 overlay (`keyboard` package now installs on a clean Mac setup; overlay position validated against connected screens, no more off-screen restores; `NSWindow.collectionBehavior`/level set via the Objective-C runtime so it follows the user across Spaces and stays above fullscreen apps) + UI polish (sidebar defaults pinned/open, social links moved to a hover-expand right rail, page-transition origin fix). 62/62 e2e passing. PySide6 fixes **not fully verified on real Mac hardware**. GitHub release published with macOS/Linux assets only (no Windows exe — see gap above). An experimental Electron-based alternative overlay was also explored on `experiment/electron-mac-overlay` (PR #1) but never merged — don't confuse its build/artifact naming with the Swift overlay below.
  - `v2.0.2` — Native Swift/NSPanel Mac overlay (`swift-overlay/`), an alternative launch path to PySide6 addressing the same Mac-overlay-reliability problem via NSPanel (always-on-top, all-Spaces, no codesigning needed) + Carbon `RegisterEventHotKey` for F2/F3 (no Input Monitoring permission needed). Embedded directly into the same `CareerCopilotPremium.app` bundle the existing `build-macos` CI job produces — one DMG, not a separate download. `premium_launcher.py` launches it on macOS when the binary is present next to `sys.executable` (frozen build) or under `swift-overlay/.build/release/` (source run), otherwise falls back to the unchanged PySide6 path; Windows/Linux untouched. Merged to `main` (`--no-ff`, merge commit `729b28d8`), `feature/swift-mac-overlay` branch deleted (local + origin). CI build verified — binary confirmed present at `Contents/MacOS/CareerCopilotOverlay` by downloading the release DMG and inspecting it with 7-Zip (Windows can't mount UDIF/HFS+ natively). GitHub release published with macOS/Linux assets only (same Windows-exe gap as v2.0.1).
  - `v2.0.3` — Swift overlay quit-menu fix: adds an `NSStatusItem` (Open Dashboard / Show Overlay / Quit) so the overlay can actually be quit — see the "2026-07-13 quit-menu fix" entry under Fix status below for the full story (the CI GUI smoke test that verified F2/F3 and NSPanel show/hide also happened between v2.0.2 and this tag, on `main`, not its own tag). Version bump commit `bf3ca170`, tag pushed and release published (run `29289468506`) with macOS/Linux assets only (same Windows-exe gap). Not yet tested on real Mac hardware — see the punch list under Fix status.
  - `v2.0.4` — Direct-download update button: the sidebar's "Check updates" flow previously opened the GitHub release page in a browser tab; now `/api/check-update` in `web_app/routes.py` also returns the release's `assets` array (`name` + `browser_download_url`, forwarded from GitHub's API response, previously discarded), and `_sidebar.html`'s JS picks the platform-specific asset by filename pattern (`.exe`/`.dmg`/`.deb`, sniffed from `navigator.userAgent`) and sets it as the "Download Now" link's `href` so clicking it downloads the installer directly instead of navigating to the release page. Falls back to the old release-page link (new tab) if no matching asset is found for the platform — currently always true for Windows, since no release has a `.exe` attached (see gap above). Version bump done in the same commit as the fix (`16cde82a`), following the established bump checklist. Tag pushed, `build-desktop-mac-linux.yml` auto-triggered on the tag push (run `29290647236`) and its `create-release` job (`softprops/action-gh-release`, `generate_release_notes: true`) published the GitHub release automatically — same CI-driven path as v2.0.1–v2.0.3, no manual `gh release create` needed. Release live with macOS/Linux assets only (same Windows-exe gap as prior releases — see gap above).
  - `v2.0.5` — **Known broken, no GitHub Release exists for this tag** — fixed by `v2.0.6`, do not build from this tag. Two changes attempted: (1) **Windows CI build**: added a `build-windows` job to `build-desktop-mac-linux.yml` (runs on `windows-latest`) that installs deps, runs `pyinstaller career-copilot.spec --clean --noconfirm` (the Windows-specific spec, distinct from `career_copilot.spec` used by mac/linux), assembles `dist\windows-bundle` (copies the PyInstaller output, renames the exe to `career-copilot.exe` to match what `installers/windows_setup.iss` expects), installs Inno Setup via `choco`, and compiles `installers/windows_setup.iss` to produce `dist\installer\CareerCopilotPremium_Setup_v2.0.5.exe`. `create-release` now `needs: [build-macos, build-linux, build-windows]`. Deliberately does **not** run the full `BUILD_CLIENT_PACKAGE.bat` (that script also assembles a full client-delivery ZIP with docs/APK/SHA256 manifest — unnecessary CI weight/failure surface for just producing the installer exe); instead it runs the same PyInstaller + Inno Setup steps that script performs internally, directly. (2) **Server-side direct-download matching**: `/api/check-update` now computes `direct_download_url` itself via `platform.system()` matched against the release's assets (`.exe`+"setup" / `.dmg` / `.deb`) instead of returning a raw `assets` array for the browser to sniff via `navigator.userAgent` — more reliable, since the Flask server *is* the user's own OS. Replaces v2.0.4's client-side `pickPlatformAssetUrl()` in `_sidebar.html`, which is now removed. **What broke**: `build-windows` failed in the `Build with PyInstaller` step (run `29291919357`) — `career-copilot.spec` lists `data/cache/.gitkeep` and `data/user_profiles/.gitkeep` as *required* (unguarded) PyInstaller `datas` entries, but both `data/cache/` and `data/user_profiles/` are gitignored (`.gitignore:47,49`), so a fresh CI checkout never has them (they only exist on a dev machine that's already run the app once). `build-macos`/`build-linux` don't hit this because `career_copilot.spec` (the mac/linux spec, a different file) wraps the same paths in an `_optional_data()` existence check. `build-windows` failing meant `create-release` (which `needs` all three build jobs) never ran at all — so this release has **zero** assets, not even the usual macOS/Linux ones. 62/62 e2e passing (app code itself wasn't broken, only the new CI job).
  - `v2.0.6` — **Also known broken, no GitHub Release for this tag either** — fixed by `v2.0.7`. Fixes `v2.0.5`'s CI failure: adds a "Prepare local data directories" step to the `build-windows` job, before the PyInstaller step, that creates `data\cache\.gitkeep` and `data\user_profiles\.gitkeep` if missing. Does not touch `career-copilot.spec` itself (its sibling `career_copilot.spec` has an explicit docstring warning not to replace it) or `.gitignore`. Version-only bump otherwise (no app-code changes since v2.0.5, so e2e suite wasn't re-run). **What broke this time**: PyInstaller succeeded, but the next step — `Build Setup.exe with Inno Setup` (run `29292325817`) — failed compiling `installers/windows_setup.iss`: `Source file "...\installers\windows\assets\vc_redist.x64.exe" does not exist`. That 25MB binary is gitignored (`.gitignore:52`, the blanket `*.exe` rule under "Large binaries"), so it's never in git; the `.iss`'s `[Files]` entry has `Check: VCRedistBundled` but that only gates *runtime* install behavior, not *compile-time* file presence — Inno Setup still needs the file physically on disk to compile, `Check:` alone doesn't add `skipifsourcedoesntexist`. Locally this always worked because `BUILD_CLIENT_PACKAGE.bat` runs `scripts/ensure_vcredist.ps1` (downloads the official MS redistributable from `https://aka.ms/vs/17/release/vc_redist.x64.exe` if missing) before calling ISCC — a step the CI job hadn't been given yet. `create-release` never ran again (same `needs` chain as v2.0.5), so this release also has zero assets.
  - `v2.0.7` — Fixes `v2.0.6`'s CI failure: adds an "Ensure VC++ redistributable" step to `build-windows`, right before the Inno Setup compile step, that runs the existing `scripts/ensure_vcredist.ps1` (same script `BUILD_CLIENT_PACKAGE.bat` already used locally) so the CI checkout has `vc_redist.x64.exe` on disk before ISCC needs it. Version-only bump otherwise (no app-code changes since v2.0.5, e2e suite not re-run — nothing in the browser-facing app changed across v2.0.5–v2.0.7, only the CI workflow). **CI-verified working** (run `29292805781`, all four jobs green including `build-windows` and `create-release`): GitHub release published with all four assets, `CareerCopilotPremium_Setup_v2.0.7.exe` included for the first time via CI. Note the Windows exe still isn't code-signed (same as the pre-existing "right-click → Open to bypass Gatekeeper"-style caveat on macOS) — Windows SmartScreen may warn on first run for users; not addressed here.
  - `v2.0.8` — Two separate commits, initially tagged inconsistently (see the tag-move note above), now reconciled. (1) **CSS-only fix** (commit `474572d1`, no Python changes per explicit request): `.sidebar-nav` changed from `flex: 1` to `flex: 0 0 auto` + 8px gap (was 10px), `.sidebar-version-area` gets `margin-top: auto` instead of relying on nav's flex-grow to push it down, `.premium-main`'s bottom padding removed (`24px 0 40px` → `24px 0 0`), and `.page-content` (shared by settings/profile/sessions/system-status) changed from `max-width: 760px; padding: 48px 32px` to `max-width: 720px; padding: 40px 32px`. **Important caveat verified with actual before/after screenshots** (`tests/e2e/screenshots/02-sidebar-expanded.png`): the sidebar/footer "blank gap" complaint that motivated the `flex:1`→`margin-top:auto` swap is **not fixed by that swap** — it's visually identical before and after, because flexbox distributes the same leftover space regardless of which element's property absorbs it. The gap is inherent to a 100vh-tall sidebar with sparse nav content pinned top+bottom (or a short page with a bottom-pinned footer); actually shrinking it would require a different visual approach (e.g. vertically centering nav instead of top-grouping it) that the user explicitly did not want. Only the content-centering change (760→720px) is a real, confirmed visual change. 62/62 e2e passing. (2) **Version bump** (commit `7b3db3e0`, requested in a follow-up message after the CSS commit): bumped `CURRENT_APP_VERSION`/`setup.py`/`career-copilot-version.txt`/`installers/windows_setup.iss` to `2.0.8`, and the `v2.0.8` tag was moved (deleted + recreated + force-pushed) from `474572d1` to `7b3db3e0` so the tag matches the version string — see the tag-move note above for why a simple new commit wasn't sufficient. **CI-verified working**: https://github.com/farazgoal-boop/career_copilot_premium/releases/tag/v2.0.8 has all four assets (`CareerCopilotPremium-mac.dmg`, `CareerCopilotPremium-linux.deb`, `CareerCopilotPremium-linux.AppImage`, `CareerCopilotPremium_Setup_v2.0.8.exe`), confirmed built from the version-bump commit (exe filename bakes in the version from `windows_setup.iss`).
  - `v2.1.0` — **Licensing security upgrade: HMAC → Ed25519.** The old scheme signed and verified activation codes with one shared secret (`ACTIVATION_SECRET`) compiled into every shipped build — extractable by decompiling the app, then reusable to mint valid codes for any machine. Ported the asymmetric scheme the sibling apps JobMind Match / MessageCannon already run: `app_licensing.py` now embeds only `PUBLIC_KEY_B64` and verifies (never signs); `scripts/generate_activation_code.py` became the full seller tool (`--init-keys`, optional customer-name arg, `issued_licenses.log` ledger) and is the only place the private key is loaded — it lives at `~/.career-copilot-license-signing/private_key.pem`, outside the repo, never shipped/committed. **Clean break, no legacy fallback** (per explicit user decision): every code issued under the old HMAC scheme fails `verify_activation_code`, so **both existing clients (Mac + Windows) must re-activate** after updating (already the norm per "Client delivery"). Request-code format (`CCP-…`) and the `activation.html` flow unchanged; `ACT-…` codes are now ~106 chars (64-byte Ed25519 signature, base32) vs 23, so `activation.html`'s input placeholder is cosmetically stale but activation still works (`_normalize_code` ignores spacing/dashes). `tests/test_licensing.py` rewritten (pytest + throwaway keypair, 10 green); `scripts/full_audit.py`'s `check_licensing()` skips the sign/activate self-test with an informational note when the seller key isn't on the machine instead of failing. `cryptography` was already a bundled dep (`desktop_app/encryption.py`, both `.spec` files list it) so no packaging change. Playwright e2e suite not re-run for this change (not installed in `.venv`/`venv311`; the change touches no e2e-exercised file — the license gate is bypassed in `tests/e2e/conftest.py`). Done on branch `feature/ed25519-licensing`, merged to `main` `--no-ff`, tag `v2.1.0` pushed with `main` → CI (`build-desktop-mac-linux.yml`) auto-builds + publishes the release. Windows exe still unsigned (SmartScreen may warn), same as prior releases.

## Key files

- `premium_launcher.py` — entry point (starts Flask + mobile bridge + Qt overlay)
- `desktop_app/overlay.py` — floating overlay window (PySide6)
- `web_app/routes.py` — Flask API/dashboard routes
- `app_licensing.py` — activation / machine licensing (Ed25519 verify-only: embeds `PUBLIC_KEY_B64`, never signs). Seller signing tool: `scripts/generate_activation_code.py` (holds the private key path + `--init-keys` + issued-license ledger)
- `desktop_app/mistral_setup.py` — AI provider (Mistral/Ollama) config
- `desktop_app/audio_handler.py` — mic/call-audio capture (sounddevice)
- `desktop_app/session_types.py` — session_type schema (`job_interview`/`client_presentation`/`product_demo`/`team_meeting`/`sales_call`) + label helpers
- `desktop_app/visual_context.py` — Visual Context Library (profile-scoped reference image storage)
- `desktop_app/session_recording.py` — continuous per-session `.webm` recording storage (session-scoped, not profile-scoped)
- `desktop_app/meeting_summary.py` — post-session summary + action items generation from `transcript_log` (Mistral, JSON mode)
- `web_app/templates/session_report.html` — post-session report page (`/session/<id>/report`)
- `desktop_app/elevenlabs_setup.py` — ElevenLabs API key storage/validation (plaintext `.env`, mirrors `mistral_setup.py`)
- `desktop_app/voice_prefs.py` — voice/speed preferences for TTS
- `desktop_app/tts.py` — server-side ElevenLabs speech synthesis (key never reaches the browser)
- `desktop_app/language_config.py` — listen/reply/speak language prefs (extended in Phase 3 with `speak_language` + German)
- `desktop_app/translation.py` — Mistral-based translation, used only when "speak to caller in" differs from the answer's language
- `web_app/static/css/premium.css` — theme tokens (`:root` = dark default, `:root[data-theme="light"]` override); all new CSS must use these `--premium-*` vars
- `web_app/templates/_sidebar.html` — sidebar nav + version badge/update-check (bottom, revealed on hover/pin)
- `swift-overlay/` — native Swift/NSPanel Mac overlay (`Package.swift` + `Sources/CareerCopilotOverlay/*.swift`: `main`, `OverlayWindow`, `AnswerView`, `FlaskClient`, `HotkeyManager`, `AppDelegate`), alternative to the PySide6 overlay on macOS. Built and embedded into `CareerCopilotPremium.app` by the `build-macos` CI job; launched from `premium_launcher.py` when present.

## Current active task

**Phase 1 — COMPLETE** (tag `phase1-complete-2026-07-07`): browser-based mic capture (Web Speech API in `web_app/static/js/browser_mic.js`) + live transcript log (`desktop_app/live_listen.py` transcript_log, surfaced in `web_app/templates/live_session.html`) + text transcript export/download (`GET /api/session/<id>/transcript/export` in `web_app/routes.py`). Scope was text-only recording, no audio capture. All 62 Playwright e2e tests passing.

**Phase 2 — COMPLETE** (tag `phase2-complete-2026-07-07`, merged to `main`, feature branch deleted):

1. Visual Context Library — image upload + Mistral vision, wired into live-session answers. **Done.**
2. Session-type-aware content generation — `session_type` field + per-type strategy/answer generation. **Done.**
3. Continuous browser session recording — one `.webm` per session (Goal #3). **Done.**
4. Meeting summary + action items generated from `transcript_log` on session end (Goal #4). **Done.** Note: built from `transcript_log` only (questions heard + Mistral's suggested replies), not a verified two-way transcript — the user's own spoken words are never captured.
5. Post-session report page at `/session/<id>/report` (Goal #5, Step 2.12) — surfaces summary, action items, transcript, and recording playback; sessions list routes ended sessions here instead of `/live`. **Done.**

**Phase 3 — COMPLETE AND SHIPPED**, merged to `main` (`--no-ff`, merge commit `4f4db0c4`), feature branch deleted. Goal: $299 → $999 tier, competing with Google Meet AI / Zoom AI Companion / Otter.ai. 10 steps total; process was one step at a time, diff shown and confirmed before each commit, full Playwright suite run at least after Steps 3.3/3.7/3.9/3.10 (and as extra insurance after any step touching shared templates) — 62/62 passing on the final pre-merge run. Also landed mid-Phase-3 (unlisted step, commit `be0694ea`): per-nav-item sidebar icon accent colors + teal update-check button restyle.

v2.0.0 GitHub release is live with Windows/macOS/Linux assets: https://github.com/farazgoal-boop/career_copilot_premium/releases/tag/v2.0.0. The Mac/Linux builds run via the `Build Mac and Linux Desktop Apps` GitHub Actions workflow (auto-triggered on the `v2.0.0` tag push, run `29073611891`, all jobs succeeded) — see Fix status below for two things that workflow surfaced that still need follow-up.

1. Real-time voice output (ElevenLabs + browser TTS fallback) — **Done** (commit `a7a1bafc`). Key stored plaintext `.env` like Mistral's. Speak button on live-session answers, Voice Output card in Settings, OS-aware VB-Cable/BlackHole guide on System Status.
2. Multi-language intelligence — **Done** (commit `6083a9f5`). Language Settings card (caller's language / answer language / speak-to-caller language, 8 languages). Speak button translates via Mistral when the target language differs from the answer's language.
3. Premium light/dark theme system — **Done** (commit `a5b4bfac`). `premium.css` token architecture (`--premium-*`, dark default + `[data-theme="light"]`), Appearance card in Settings, no-flash theme-init in `base.html`. `activation.html` deliberately excluded (own palette, never-touch per rules below).
4. Auto-update notification in sidebar — **Done** (commit `bb9e6a6c`). `GET /api/check-update` against the real GitHub Releases API, manual/click-triggered only.
5. Company/client research before sessions — **Done** (commit `ddbcd36f`). `desktop_app/company_research.py` (Mistral, JSON mode) generates an overview/focus-areas/culture-values/smart-questions briefing, auto-triggered from quick-start when a real company name is given (best-effort, never blocks session start) plus `GET/POST /api/session/<id>/research[/generate]`. Collapsible "Company research" card on `live_session.html`, "Add company details for research" modal on `index.html`. Briefing is LLM training-knowledge, not a live lookup — caveat surfaced in the UI.
6. Resume-matched personalized answers — **Done** (commit `1a6a83f8`). `strategy_generator.py`'s `build_resume_highlights()` flattens every skill/job-achievement/project (not just top-3/first) into keyword-tagged `ResumeHighlight`s on `StrategyPack`; `answer_builder.py` token-matches the live question against all of them and injects the most relevant specific experience into the prompt instead of always the same canned template.
7. Interview Preparation Mode (`/prepare`) — **Done** (commit `3fc09379`, 62/62 Playwright passing). New session-less page sourced from the saved briefing/profile: readiness score/band/action-plan (`ConfidenceAssessment`, first surfaced in UI here), full resume-highlight list (Step 3.6), on-demand company research via `POST /api/prepare/research` (session-less variant of Step 3.5), all 15 session-type practice questions as difficulty-grouped flashcards, and a "Start session now" CTA into quick-start. Sidebar link added in `_sidebar.html`.
8. Premium onboarding experience — **Done** (commit `17fb89e5`, 62/62 Playwright passing). Fixed a real substance bug: onboarding previously built the entire structured profile from hardcoded placeholders regardless of the uploaded resume, so Steps 3.6/3.7 worked off fake data for every real user. `desktop_app/resume_profile_extractor.py` (Mistral JSON-mode) now extracts real skills/work-history/projects from resume text, with a minimum-content check and clean fallback to the old placeholder path on any failure (missing key, sparse resume, LLM error — onboarding never blocks). Also added DOCX upload support (was PDF-only), drag-and-drop with filename confirmation, and a skills-preview chip list on the done step.
9. Performance + polish pass — **Done** (commit `30a8c136`, 62/62 Playwright passing). Signature shatter-out (8x6 grid, canvas/html2canvas capture) + 3D flip-in (rotateX/rotateY, easeOutBack overshoot) page transition, originating from the clicked sidebar item. `web_app/static/js/transition.js` (new, `CC_TRANSITION.runOut()` only — multi-page app, so the IN half animates the real `.page-content` directly on load, no capture needed). Wired into `_sidebar.html`'s nav click handler and `base.html`'s entry animation; `perspective`/reduced-motion rules in `premium.css`. Respects `prefers-reduced-motion` on both halves; degrades to a fast opacity fade if html2canvas fails to load (verified navigation is never blocked).
10. Final v2.0.0 build — **Done** (commit `cfb9270e`, 62/62 Playwright passing, tagged `v2.0.0` locally). Version bumped consistently across `web_app/routes.py` (`CURRENT_APP_VERSION`), `setup.py`, `career-copilot-version.txt`, and `installers/windows_setup.iss`. Multi-platform packages (Windows exe build, Mac zip) deliberately **not** run yet — see "release not finished" note above.

Never touch (Phase 3 additions, on top of the list below): `desktop_app/audio_handler.py`'s F2 path, Playwright test suite *structure*. If anything breaks mid-step: `git checkout -- <file>` and report, don't silently patch over it.

Resume prompt for a fresh session: "Read CLAUDE.md. Phase 3 is complete and shipped (v2.0.0). v2.0.1 (PySide6 Mac-overlay fixes + UI polish), v2.0.2 (native Swift/NSPanel Mac overlay, embedded in the same DMG), v2.0.3 (Swift overlay quit-menu fix), v2.0.4 (sidebar update button downloads the platform installer directly instead of opening the GitHub release page), v2.0.5 (attempted a Windows CI build job so a `.exe` finally gets attached to releases, plus moved the update-button's platform matching server-side — but failed on a missing gitignored `data/` scaffold file), v2.0.6 (fixed that, but hit a second failure — a missing gitignored `vc_redist.x64.exe` the Inno Setup script needs at compile time), v2.0.7 (fixes that by running the existing `scripts/ensure_vcredist.ps1` in CI, CI-confirmed working, first fully-automated Windows+Mac+Linux release since v2.0.0), and v2.0.8 (CSS-only sidebar/footer/content-centering spacing tweaks, plus a version bump that required deleting and recreating the v2.0.8 tag -- it was first pushed against a commit that hadn't bumped CURRENT_APP_VERSION, then moved once the bump landed, so check `git log` rather than assuming the first v2.0.8 push is what's live) have since shipped on top of it, all merged to main. v2.0.0–v2.0.4 and v2.0.7–v2.0.8 have confirmed published GitHub releases; v2.0.5 and v2.0.6 are both known-broken (no release, same Windows-CI debugging arc, superseded by v2.0.7). Windows exe still isn't code-signed, so SmartScreen may warn on first run; not addressed yet. Also worth knowing: v2.0.8's sidebar-gap CSS change was confirmed via screenshot to be visually a no-op (see v2.0.8's tag-entry note) -- don't assume "spacing was fixed" from the commit message alone. A CI smoke test confirmed the Swift overlay's build/launch/NSPanel show-hide/Carbon F2-F3 hotkeys genuinely work on real macOS hardware with no permission prompt, and v2.0.3 added a menu-bar quit path (Open Dashboard / Show Overlay / Quit) that didn't exist before. Five things still need a human on physical Mac hardware before this can be called done: Gatekeeper's unsigned-app warning, real fullscreen/multi-Space behavior, the launcher-to-overlay handoff in the actual packaged app, the Flask integration during a real session, and visually confirming the new status item. That handoff is the actual next gate, not new feature work. Phase 4 not yet defined — confirm scope with user before starting new work."

## Fix status

**FIXED:**
- Activation persistence
- Profile save
- Sessions table UI
- Mobile QR browser companion
- Ollama optional labels
- Audio platform text (BlackHole/Stereo Mix/PulseAudio)
- Single-instance reactivation (`premium_launcher.py`)
- System tray quit (`premium_launcher.py`)
- Activation crypto hardened — Ed25519 (branch `feature/ed25519-licensing`, 2026-08-30): ported the same asymmetric scheme the sibling apps JobMind Match / MessageCannon already use. Replaces the shared-secret HMAC (`ACTIVATION_SECRET`, which both signed and verified and was compiled into every shipped build — extractable, reusable to mint codes for any machine). `app_licensing.py` now embeds only `PUBLIC_KEY_B64` and can verify; `scripts/generate_activation_code.py` became the full seller tool (`--init-keys`, optional customer-name arg, `issued_licenses.log` ledger) and is the ONLY place the private key is loaded — it lives at `~/.career-copilot-license-signing/private_key.pem`, outside the repo, never shipped/committed. **Clean break, no legacy fallback**: every activation code issued under the old HMAC scheme fails `verify_activation_code`, so both existing clients (Mac + Windows) must re-activate after this update (already the norm per "Client delivery" below). Request-code format (`CCP-…`, still HMAC-free SHA256 of the machine fingerprint) and the `activation.html` flow are unchanged; the `ACT-…` codes are now ~106 chars (64-byte Ed25519 signature, base32) instead of 23, so `activation.html`'s input placeholder is cosmetically stale but activation still works (`_normalize_code` ignores spacing). `tests/test_licensing.py` rewritten (pytest + throwaway keypair, 10 tests green); `scripts/full_audit.py`'s `check_licensing()` skips the sign/activate self-test with an informational note when the seller key isn't on the machine instead of failing. `cryptography` was already a bundled dep (`desktop_app/encryption.py`, both `.spec` files) so no packaging change. Shipped as **v2.1.0** (minor bump — breaks all existing activation codes). Seller private key generated on the dev machine at `~/.career-copilot-license-signing/private_key.pem` — **the only copy; if lost, no valid activation codes can ever be issued again**.

**PENDING:**
- Mac overlay (PySide6 on Mac unreliable) — investigated 2026-07-09: there is no mic+Ollama+audio gate blocking overlay launch (it already always opens once PySide6 imports; already has a status bar + language selector). Real suspected mechanism: `run_first_time_setup_wizard()`'s modal `QDialog` (`desktop_app/mistral_setup.py`) was never raised/activated before `exec()`, so on macOS (launched from a shell script, not a real `.app` bundle) it can render unfocused/behind other windows, blocking `main()` before the overlay is ever reached. Fixed by scheduling `raise_()`/`activateWindow()` via `QTimer.singleShot` before `exec()` (commit `818bf803`) — **still not verified on an actual Mac** as of the v2.0.0 release (2026-07-10). The `v2.0.0` CI run (`build-macos` job, run `29073611891`) only proves the app *packages* into a DMG successfully — it doesn't launch or exercise the app, so it says nothing about this fix. `v2.0.1` added further PySide6 fixes (`keyboard` install path, on-screen position validation, `NSWindow.collectionBehavior`/level via the Objective-C runtime) — also unverified on real hardware. `v2.0.2` took a second, parallel approach: a native Swift/NSPanel overlay (`swift-overlay/`) needing no codesigning or permissions, embedded in the same DMG as an alternative launch path (see Version section above).
  - **2026-07-13 smoke test (post-v2.0.2, on `main`, not tagged):** added `.github/workflows/test-swift-overlay-macos.yml` (`workflow_dispatch`-only) which builds and launches the Swift overlay on the `macos-latest` runner — real macOS hardware/VM with an active GUI session, not just a packaging check — then simulates F2/F3 via `osascript`/System Events and captures a screenshot + log. First attempt (run `29287320010`) had two harness bugs, both fixed in commit `737c84d5`/merge `e934ab63`: `overlay.log` came back empty (Swift's `print()` is block-buffered off a terminal and `SIGTERM` never flushed it — fixed with `setbuf(stdout, nil)` in `main.swift`), and a second `screencapture` call in one session triggered an unattended macOS screen-recording consent dialog that papered over the real screen (fixed by dropping to one screenshot per run). Re-run (`29287827253`) confirmed for real: the log shows `F2 pressed — triggering listen` / `F3 pressed — toggling visibility` for the simulated keypresses (proving the Carbon-registered hotkeys are genuinely global, with **no permission prompt**, unlike PySide6's `keyboard` package), and the screenshot shows the NSPanel actually disappeared after the F3 toggle. **Build, launch, NSPanel show/hide, and Carbon F2/F3 hotkeys are now CI-verified working on real macOS.** The workflow file itself was deleted afterward (commit follows this one) now that it served its purpose — recreate it from this description (or `git log -p` on this path) if `swift-overlay/` changes again and needs re-verifying.
  - **2026-07-13 quit-menu fix (commit `34bdd47e`, released as `v2.0.3`):** found by re-reading the code, not by testing — the app runs with `.accessory` activation policy (no dock icon, no menu bar), and `_run_swift_overlay()` in `premium_launcher.py` never set up an equivalent to the PySide6 path's tray icon/Quit menu item. The only way to stop it was Force Quit via Activity Monitor. Fixed by adding an `NSStatusItem` in `AppDelegate.swift` (Open Dashboard / Show Overlay / Quit); Quit calls `NSApplication.shared.terminate(nil)`, which ends the Swift process and makes `premium_launcher.py`'s `process.wait()` return so `services.stop_services()` runs in its `finally` block — no Python changes needed. Verified via `build-macos` (run `29288714414`) that `swift build -c release` still compiles cleanly before merging; **not yet verified that the status item actually appears/works on a real Mac** (no GUI smoke test was re-run for this specific change). Version bump + tag/release in commit `bf3ca170` / `v2.0.3` (run `29289468506`).
  - **What's still not covered by CI, and is the actual remaining gate before handing to a real Mac tester:**
    1. Gatekeeper's unsigned-app warning on a real user's first double-click of a downloaded DMG (CI runs the binary directly with no quarantine flag) — expected; right-click → Open bypasses it, same as the existing PySide6 build.
    2. Genuine above-fullscreen/multi-Space behavior — no fullscreen app was running in any CI test to check against.
    3. The launcher → overlay handoff has never been exercised — CI only ever ran the compiled Swift binary directly, never the actual packaged `CareerCopilotPremium.app` confirming `premium_launcher.py` finds the binary via `os.path.dirname(sys.executable)` and spawns it successfully.
    4. The Flask integration has never been exercised end-to-end — CI ran the overlay with no Flask server behind it, so `FlaskClient` never displayed a real answer; nobody has confirmed the overlay shows live Q&A during an actual session, or that F2 triggers a listen cycle end-to-end.
    5. The new status item (Open Dashboard / Show Overlay / Quit) itself hasn't been visually confirmed on a real Mac yet.

    All five need a human on physical Mac hardware — that's what's left before closing this item. If it passes, the Swift overlay likely supersedes the PySide6 investigation entirely as the shipped default.
- Mac codesigning/notarization not yet set up, and one known blocker for it: the `v2.0.0` macOS CI build (`build-macos` job, run `29073611891`) logged `speech_recognition`'s bundled `flac-mac` binary as having an "invalid or incompatible macOS SDK version," warning it "will likely cause issues with code-signing and hardened runtime." Not a problem today (README already documents the app as unsigned — users right-click → Open to bypass Gatekeeper), but flag this binary first if codesigning/notarization is ever attempted.
- System slowdown (no root cause found yet)

## Safety rules

- Always diagnose before changing anything.
- One feature at a time, git branch per feature.
- Never touch: SSL fix, backup tags, activation code generation logic (including `activation.html`'s UI/palette), Playwright test suite structure, `desktop_app/audio_handler.py`'s F2 path.
- Always confirm with user before committing.

## Client delivery

- Mac: script-based zip (`DELIVER_TO_CLIENT/Mac/`)
- Windows: `CareerCopilotPremium_Setup_vX.X.X.exe`
- Both clients need re-activation after major updates.

## Before starting work

DO NOT start new work without reading this file.
