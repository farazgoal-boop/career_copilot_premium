# Kivy Prototype Surface

This folder remains a prototype-only path.

The shared contract for any Kivy proof-of-concept is the same mobile bridge snapshot generated by `mobile_app.runtime_bridge`.

The Android-targeted runtime plan lives in `mobile_app.android_runtime` and should remain the source of truth for permissions and service naming.